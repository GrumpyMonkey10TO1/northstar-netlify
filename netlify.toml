[build]
  publish   = "netlify"            # your index.html is inside /netlify
  functions = "netlify/functions"  # chat.js & hello.js are inside /netlify/functions
  command   = ""                   # no framework build

[functions]
  node_bundler = "esbuild"

# Add CORS headers for your functions
[[headers]]
  for = "/.netlify/functions/*"
  [headers.values]
    Access-Control-Allow-Origin = "https://migratenorth.ca"
    Access-Control-Allow-Methods = "POST, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type, Authorization"
    Access-Control-Max-Age = "86400"

# Redirect so that migratenorth.ca/.netlify/functions/* points to your deployed functions
[[redirects]]
  from = "/.netlify/functions/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true









