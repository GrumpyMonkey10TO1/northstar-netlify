# netlify.toml
[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# Send any route to index.html so you never get a blank page
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
