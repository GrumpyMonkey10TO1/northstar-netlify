// Stripe → Supabase Entitlement Bridge
// Migrate North Academy

export async function handler(event) {
  return {
    statusCode: 200,
    body: "Stripe webhook placeholder file created successfully"
  };
}
